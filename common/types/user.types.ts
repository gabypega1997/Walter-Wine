export type SignInData = {
    email: string;
    password: string;
};

export type SignupFormData = {
    displayName: string;
    email: string;
    password: string;
    confirmPassword: string;
};
